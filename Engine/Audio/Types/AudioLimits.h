#pragma once

namespace Engine
{
    namespace AudioLimits
    {
        inline constexpr float MinVolume = 0.0f;

        inline constexpr float MaxVolume = 1.0f;

        inline constexpr float MinPan = -1.0f;

        inline constexpr float MaxPan = 1.0f;

        inline constexpr float MinPitch = 0.25f;

        inline constexpr float MaxPitch = 4.0f;
    }
}